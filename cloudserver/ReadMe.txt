Source code of the dynamic web project is in the cloud folder.
cloud.war is the export product of the web project.
Apache Tomcat is the server(servlet container).
Put the cloud.war into webapps folder in Tomcat and run the startup.bat in bin folder, then the server should work.
Run the shutdown.bat in bin to stop the server.